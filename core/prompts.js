/**
 * LLM Prompt 模板
 */

// L1 删废话（粗筛）
export const DELETE_FILLER_PROMPT = `你是一个角色卡 token 优化专家。压缩以下角色描述文本，在不丢失核心人设的前提下最大化减少 token 数量。

## 规则
1. 删除不影响角色演绎的非核心细节（如"她喜欢蓝色"）
2. 将散文段落压缩为关键词列表
3. 合并语义重叠的描述
4. 保留定义性的核心特征（伤疤、口头禅、核心动机等）
5. 输出纯文本，不要添加解释`;

// L2 类型化压缩（按文本类型路由）
export const TYPE_SPECIFIC_PROMPT = {
    personality: `你是一个角色卡结构化专家。将以下性格描述压缩为最核心的性格关键词。
只保留定义角色人设的关键词，去掉所有修饰语和例子。每个关键词不超过4个字。`,
    appearance: `你是一个角色卡结构化专家。将以下外貌描述压缩为最核心的外貌关键词。
只保留最具辨识度的外貌特征（如伤疤、发色等），去掉常见描述。每个关键词不超过4个字。`,
    backstory: `你是一个角色卡结构化专家。将以下背景故事压缩为关键事件和因果。
只保留影响角色行为的核心背景，去掉叙事细节。使用"事件→影响"格式。`,
    relationship: `你是一个角色卡结构化专家。将以下关系描述压缩为关系三元组。
格式：(角色A, 关系, 角色B)，如 (林夏, 闺蜜, 陈雨)。`,
    dialogue_style: `你是一个角色卡结构化专家。将以下说话方式描述压缩为关键特征。
只保留最具辨识度的说话特征和口癖。`,
    default: `你是一个角色卡结构化专家。将以下内容压缩为关键信息。
去掉所有冗余描述，只保留核心事实。`,
};

// L3 结构化（PList 格式）
export const PLIST_FORMAT_PROMPT = (tokenTarget = 150) => `将以下压缩后的描述转为 PList 格式。

## 格式要求
每行格式：\`- 类别: 描述1, 描述2, 描述3;\`
描述可嵌套：\`- 外貌: 银发, 疤(左眉);\`
用方括号包裹整个内容：\`[- ...;]\`

## 类别参考
原型, 龄, 外貌, 性格, 语, 能力, 缺陷, 关系, 背景, 偏离原型

## 规则
1. 每个描述不超过4个字
2. 同类描述用逗号分隔，行末分号
3. 只保留最核心的特征
4. 总 token 目标 ≤ ${tokenTarget}`;

// L4 原型锚点
export const ARCHETYPE_PROMPT = `请先识别角色卡对应的文化原型（如：硬汉侦探、傲娇学妹、中二反派、温柔御姐、冷酷杀手、天真少女...）。
然后在 PList 中添加 "- 原型: xxx;" 行。
如果角色有偏离原型的设定，添加 "- 偏离原型: xxx;" 行。
原型默认行为不需要写出，AI 会自动从原型词推导。`;

// L5 一句话总结
export const SUMMARY_PROMPT = `为以下角色在末尾添加一行总结，格式：
"- 一句话: [最精炼的角色概括]"

总结要求：
1. 用一句话概括角色最核心的人设张力
2. 不超过15个字
3. 保留角色的核心矛盾或独特之处`;
